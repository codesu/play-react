const path = require('path');

const PRJ_PATH = {
    srcRoot: path.resolve(__dirname, '../../src'),
    distRoot: path.resolve(__dirname, '../../dist'),
    componentRoot: path.resolve(__dirname, '../../src/component'),
}

module.exports = PRJ_PATH;
